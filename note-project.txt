1. Create venv -> python -m venv venv
2. activate venv -> venv\Scripts\activate for mac source venv/bin/activate
3. instaill libraries -> pip install -e .
4. Create data_ingestion.py
5. Create data_processing.py
6. Create data_model.py
7. Tracking using MLFLOW and DAGSHUB
   - run -> mlflow ui


Use githup
git status
git add .
git add filename.py
git commit -m "Add deep learning notebook for IBM ML course"
git commit -m "Fix data preprocessing pipeline in Google Analytics module"
git push origin main